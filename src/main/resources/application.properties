# Datasource configuration
spring.datasource.url=jdbc:mysql://${DATABASE_HOST:mysql-3ce7dc47-bankapp.c.aivencloud.com}:${DATABASE_PORT:21720}/${DATABASE_NAME:banksimfull}
spring.datasource.username=${DATABASE_USER:avnadmin}
spring.datasource.password=${DATABASE_PASSWORD:AVNS_zT9JuQ06XL8funyStCd}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Messages configuration
spring.messages.basename=i18n/messages


